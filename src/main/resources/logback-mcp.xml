<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender that outputs to STDERR -->
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender for persistent logging -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./logs/spring-boot-ai-mongo-mcp-server.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>./logs/spring-boot-ai-mongo-mcp-server.log.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Silence noisy loggers -->
    <logger name="org.springframework" level="ERROR"/>
    <logger name="org.mongodb" level="ERROR"/>
    <logger name="org.apache" level="ERROR"/>
    <logger name="com.mongodb" level="ERROR"/>
    
    <!-- Keep our application logs at INFO level -->
    <logger name="com.deepai" level="INFO"/>

    <!-- Root logger configuration -->
    <root level="ERROR">
        <appender-ref ref="STDERR"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
